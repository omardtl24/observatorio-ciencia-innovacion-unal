# Frontend example environment variables (Vite)
# Set the API base URL the frontend will call in development
VITE_API_URL=http://localhost:5000

# If your backend runs on a different host/port, update the URL above.