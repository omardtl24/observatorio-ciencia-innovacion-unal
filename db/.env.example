# PostgreSQL example environment variables for local development
# Replace the values below with secure credentials in production.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=observatorio
POSTGRES_PORT=5432
